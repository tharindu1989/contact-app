FROM mysql:latest

ARG DATABASE_PASSWORD
ARG DATABASE_NAME

RUN echo "Database Password: ${DATABASE_PASSWORD}"
RUN echo "Database Name: ${DATABASE_NAME}"

ENV MYSQL_ROOT_PASSWORD=${DATABASE_PASSWORD}
ENV MYSQL_DATABASE=${DATABASE_NAME}

EXPOSE 3306